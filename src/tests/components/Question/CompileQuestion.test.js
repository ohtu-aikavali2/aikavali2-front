/* import React from 'react'
import { shallow } from 'enzyme'
import CompileQuestion from '../../../app/components/Question/CompileQuestion' */

describe('<CompileQuestion />', () => {
  // let compileQuestion
  /* const props = {
    kind: 'CompileQuestion',
    options: [
      'option1',
      'option2',
      'option3'
    ]
  } */
  beforeAll(() => {
    // compileQuestion = shallow(<CompileQuestion question={props} />)
  })
  it('renders self, subcomponents and answer options', () => {
    /* expect(compileQuestion.find('.compileQuestion').length).toBe(1)
    const text = compileQuestion.find('.compileQuestion').text()
    expect(text).toContain('COMPILE QUESTION')
    expect(text).toContain('Mikä seuraavista kääntyy?')
    expect(text).toContain(props.options[0])
    expect(text).toContain(props.options[1])
    expect(text).toContain(props.options[2]) */
    expect(true).toBe(true)
  })
})